import { PageHero } from "@/components/page-hero"; import { demoInactiveManagers } from "@/data/managers"; import { demoCareerRecords } from "@/data/mock";
export const metadata={title:"CBA Graveyard"}; export default function Page(){return <><div className="border-b border-[#29323a] bg-[radial-gradient(circle_at_50%_0,#28312e,#080c0d_65%)]"><PageHero eyebrow="Gone from the league, never erased" title="CBA Graveyard" intro="A permanent resting place for former managers and the franchises they left behind. Inactive identities will be discovered during ESPN import—not discarded."/></div><section className="container mt-12"><div className="mb-6 flex justify-end"><span className="demo-badge">Placeholder identity only</span></div>{demoInactiveManagers.map(m=>{const r=demoCareerRecords.find(x=>x.managerId===m.id)!;return <article className="surface grid gap-6 border-l-4 border-l-[#66746f] p-7 lg:grid-cols-[1fr_2fr]" key={m.id}><div><p className="eyebrow">Seasons {m.firstSeason}–{m.lastSeason}</p><h2 className="display mt-3 text-5xl text-[#c3cbc6]">{m.displayName}</h2><p className="mt-2 text-[#84918d]">{m.fullName}</p></div><div className="grid grid-cols-2 gap-5 sm:grid-cols-4"><div><p className="text-xs text-[#78857f]">Career</p><b className="stat-number text-2xl">{r.wins}-{r.losses}</b></div><div><p className="text-xs text-[#78857f]">Win %</p><b className="stat-number text-2xl">{(r.wins/(r.wins+r.losses)).toFixed(3)}</b></div><div><p className="text-xs text-[#78857f]">Playoffs</p><b className="stat-number text-2xl">{r.playoffAppearances}</b></div><div><p className="text-xs text-[#78857f]">Titles</p><b className="stat-number text-2xl">{r.championships}</b></div><div className="col-span-full border-t border-[#303a36] pt-4"><p className="text-xs text-[#78857f]">Historical team names</p><p className="mt-1 text-sm">Pending ESPN identity match</p></div></div></article>})}<p className="mt-6 text-xs text-[#6f7d78]">Commissioner epitaph field is supported by the database schema and intentionally left blank.</p></section></>}
