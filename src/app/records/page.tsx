import{PageHero}from"@/components/page-hero";import{cbaData}from"@/data/real";
export const metadata={title:"Record Book"};export default function Page(){return <><PageHero eyebrow="League records" title="Record Book" intro="The best, worst, closest, and most ridiculous eligible marks in CBA history."/><section className="container grid gap-4 md:grid-cols-2">{cbaData.records.map((r,i)=><article className="surface p-6" key={r.label}><p className="eyebrow">Record {String(i+1).padStart(2,"0")}</p><h2 className="mt-5 text-xl font-bold">{r.label}</h2><p className="stat-number mt-4 text-4xl text-[#dfb65f]">{typeof r.value==="number"?Number(r.value.toFixed(4)).toLocaleString():r.value}</p><p className="mt-2 text-sm text-[#929ba5]">{r.managerName}{r.year?` · ${r.year}${r.week?` Week ${r.week}`:""}`:""}</p></article>)}</section></>}
