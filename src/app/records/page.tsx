import{PageHero}from"@/components/page-hero";import{cbaData}from"@/data/real";
export const metadata={title:"Record Book"};export default function Page(){return <><PageHero eyebrow="The standard" title="Record Book" intro="Verified marks calculated from completed ESPN matchups and seasons. No projected or fabricated records."/><section className="container grid gap-4 md:grid-cols-2">{cbaData.records.map((r,i)=><article className="surface p-6" key={r.label}><p className="eyebrow">Record {String(i+1).padStart(2,"0")}</p><h2 className="display mt-5 text-3xl">{r.label}</h2><p className="stat-number mt-5 text-4xl text-[#e4bd65]">{typeof r.value==="number"?Number(r.value.toFixed(4)).toLocaleString():r.value}</p><p className="mt-2 text-sm text-[#8e9ba3]">{r.managerName}{r.year?` · ${r.year}${r.week?` Week ${r.week}`:""}`:""}</p></article>)}</section></>}
