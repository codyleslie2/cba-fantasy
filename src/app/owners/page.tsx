import Link from "next/link"; import { PageHero } from "@/components/page-hero"; import { activeManagers } from "@/data/managers";
export const metadata={title:"Owners"}; export default function Page(){return <><PageHero eyebrow="The people behind the teams" title="Owner Directory" intro="Fourteen active managers, one league, and a permanent identity that survives every team-name change."/><section className="container grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{activeManagers.map((m,i)=><Link href={`/owners/${m.slug}`} key={m.id} className="surface group p-6 transition hover:-translate-y-1 hover:border-[#927a3e]"><div className="flex items-start justify-between"><span className="grid size-12 place-items-center rounded-full border border-[#46545e] bg-[#18232c] font-black text-[#d5a846]">{m.fullName.split(" ").map(n=>n[0]).join("")}</span><span className="text-xs text-[#72808a]">#{String(i+1).padStart(2,"0")}</span></div><h2 className="display mt-8 text-4xl">{m.displayName}</h2><p className="mt-1 text-sm text-[#89969e]">{m.fullName}</p><p className="mt-6 text-xs font-bold uppercase tracking-widest text-[#83cda8]">Active manager →</p></Link>)}</section></>}
